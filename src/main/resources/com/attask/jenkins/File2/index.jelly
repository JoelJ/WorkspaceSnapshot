<j:jelly xmlns:j="jelly:core" xmlns:st="jelly:stapler">
	<div class="file">
		<span class="name">${it.name}</span>
		<span class="size" data="${it.getSize()}">${it.getPrettySize()}</span>
		<span class="lastModified">${it.lastModified}</span>
	</div>
</j:jelly>
